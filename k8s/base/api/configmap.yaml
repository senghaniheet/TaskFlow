apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
data:
  NODE_ENV: "production"
  PORT: "5000"
  MONGO_URI: "mongodb://mongo:27017/taskflow"
  JWT_EXPIRES_IN: "1d"
  ALLOWED_ORIGINS: "http://localhost:3000"
